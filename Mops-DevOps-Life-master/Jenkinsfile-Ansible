pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('checkout'){
            steps {
                cleanWs()
                script {
                    git credentialsId: 'devopint', url: 'https://github.com/DevOpsINT/Course.git/'
                }
            }
        }
        stage('Ansible Playbook') {
            steps {
                dir ('Ansible-Project/mops'){
                script { 
                targetIP = input(
                            id: 'userInput', message: 'ip add',
                            parameters: [
                                string(defaultValue: '0.0.0.0',
                                       description: 'ip address',
                                       name: 'IP')])
                    
                    sh "sed -i 's/HOST_IP/${targetIP}/g' hosts"
                    sh "ansible-playbook -i hosts  -u ubuntu -b --private-key=/opt/pems/Server16.pem Mops-DevOps-Life-master/Ansible-Project/mops/main.yml"
                    sh 'cat hosts'
                    sh "ansible-playbook -i hosts  -u ubuntu -b --private-key=/opt/pems/Server16.pem playbook.yml"
                    
                }
            }
        
        }
    }
}
}


           
                     
                
          
